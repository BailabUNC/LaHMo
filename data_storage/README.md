This folder stores local data generated by the desktop acquisition app.

- `sessions/` contains CSV logs from BLE acquisition runs.
- Session CSV files are intentionally git-ignored.
- Keep this README in version control so the folder purpose remains clear.
